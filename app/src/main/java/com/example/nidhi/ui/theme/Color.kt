package com.example.nidhi.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Color palette for the Nidhi app.
 * Green-themed color system inspired by Urban Company.
 */

// Primary Green Colors
val Green500 = Color(0xFF22C55E)        // Primary brand color
val Green600 = Color(0xFF16A34A)        // Darker primary
val Green50 = Color(0xFFD1FAE5)         // Primary container/light background
val Green100 = Color(0xFFBBF7D0)        // Light accent

// Background & Surface
val Background = Color(0xFFF8FAF8)      // Subtle green-tinted white
val Surface = Color(0xFFFFFFFF)         // Pure white for cards
val SurfaceVariant = Color(0xFFF1F5F9)  // Light gray for secondary surfaces

// Text Colors
val OnSurface = Color(0xFF0F172A)       // Primary text (dark slate)
val OnSurfaceVariant = Color(0xFF64748B) // Secondary text (muted slate)
val OnPrimary = Color(0xFFFFFFFF)       // Text on primary color

// Status Colors
val Success = Color(0xFF22C55E)         // Green - success, completed
val Warning = Color(0xFFF59E0B)         // Amber - warning, pending
val Error = Color(0xFFEF4444)           // Red - error, cancelled
val Info = Color(0xFF3B82F6)            // Blue - info

// Additional UI Colors
val Divider = Color(0xFFE2E8F0)         // Light dividers
val Outline = Color(0xFFCBD5E1)         // Input borders
val OutlineVariant = Color(0xFFE2E8F0)  // Subtle borders

// Accent Colors (for gradients and highlights)
val Teal400 = Color(0xFF2DD4BF)
val Blue500 = Color(0xFF3B82F6)
val Purple500 = Color(0xFF8B5CF6)
val Orange400 = Color(0xFFFB923C)
